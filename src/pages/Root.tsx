import { Box, Typography } from "@mui/material";

const RootPage = () => {
  return (
    <Box sx={{ display: "flex", justifyContent: "center", m: "16px" }}>
      <Typography variant="h5">Welcome to the Intellectsoft test app</Typography>
    </Box>
  );
};

export default RootPage;
